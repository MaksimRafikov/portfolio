# Сайт-портфолио — Максим Рафиков

Личный сайт для заказчиков и поиска: главная, кейсы и посадочные услуг.
Индексация открыта (`index,follow`), есть `robots.txt`, `sitemap.xml`, Open Graph и JSON-LD.

Статика: чистый HTML + CSS, JS — шапка, меню, форма, показ контактов.
Сборка не нужна, зависимостей нет.

## Живая ссылка

- **Сайт:** https://maximrafikov.ru/
- **Зеркало GitHub Pages:** https://maksimrafikov.github.io/portfolio/
- **Репозиторий:** https://github.com/MaksimRafikov/portfolio

Деплой: GitHub Pages из ветки `main`, корень репозитория, свой домен `maximrafikov.ru`.
После правок — `git push`; Pages обновляется за минуту-две.

Форма «Описать задачу» идёт через Formsubmit на gmail.
Первая заявка с нового домена может потребовать подтверждения письма на почте — один раз после смены домена стоит отправить тестовую.

## Как открыть локально

Самый простой способ — открыть `index.html` двойным кликом, браузер покажет сайт как есть.

Если нужен «честный» локальный адрес (например, посмотреть с телефона в той же сети):

```powershell
cd "c:\Users\Пользователь\Desktop\Coursor\сайт-портфолио"
python -m http.server 5173
```

Дальше — `http://localhost:5173` в браузере.

Шрифты (Source Serif 4, Golos Text, IBM Plex Mono) подгружаются с Google Fonts.
Без интернета сайт откроется на запасных шрифтах — вёрстка не ломается,
но перед показом на встрече лучше один раз открыть страницу с сетью.

## Структура

```
index.html                 главная: кто я, ситуации, кейсы, процесс, контакты
cases/*.html               8 страниц кейсов
uslugi/*.html              посадочные услуг (парсинг, боты, AI, госзакупки)
robots.txt / sitemap.xml   для поисковиков
assets/css/site.css        дизайн-система + стили
assets/js/site.js          шапка, меню, форма, контакты
assets/img/og-default.png  превью для соцсетей и сниппетов
assets/img/previews/       SVG-схемы кейсов
assets/favicon.svg         иконка вкладки
```

## Дизайн-система

Все решения — в блоке токенов в начале `assets/css/site.css`. Менять цвета, шрифты и
отступы нужно там, а не в отдельных правилах.

- **Цвет.** Фон Cloud Dancer (PANTONE 11-4201) `--paper` `#f0eee9`, чернила `--ink` `#16181b`,
  единственный акцент `--accent` `#a6431e` (жжёная сиена). Есть тёмная тема (`data-theme="dark"`).
- **Шрифты.** Заголовки — Source Serif 4 (`--font-display`), текст — Golos Text
  (`--font-body`), цифры и служебные метки — IBM Plex Mono (`--font-mono`).
- **Размеры.** Через `clamp()`, поэтому отдельных мобильных медиазапросов на
  типографику нет.
- **Отступы.** Шкала `--space-3xs … --space-3xl`, вертикальный ритм секций — `--space-2xl`.
- **Вёрстка.** Mobile-first: базовые стили — телефон, брейкпоинты `46rem` и `62rem`.

Сознательно не используется: градиенты, glassmorphism, карточки с тенями, эмодзи,
придуманная статистика, Inter/Roboto.

## Правила по контенту

- Цифры — только из `КЕЙСЫ_ДЛЯ_САЙТА_И_БИРЖ.md` (папка `Портфолио для бирж`).
  Ничего не округлять «для красоты» и не выдумывать.
- Не публиковать: имена клиентов без согласия, таблицы с живыми email и телефонами,
  логины, токены ботов.

## Этапы

- **Этапы 1–4 закрыты (21.09.2026).** Каркас, контент, 8 кейсов, превью и доводка.
- **Этап 5 закрыт (21.09.2026).** Деплой на GitHub Pages + короткая ссылка clck.ru.
- **Свой домен (23.09.2026):** https://maximrafikov.ru/
- **SEO (23.09.2026):** снят `noindex`, sitemap, OG/Schema, 4 страницы услуг.

После деплоя: подключить [Яндекс.Вебмастер](https://webmaster.yandex.ru) и [Google Search Console](https://search.google.com/search-console), отправить `https://maximrafikov.ru/sitemap.xml`. В Pages включить **Enforce HTTPS**, когда сертификат станет зелёным.
